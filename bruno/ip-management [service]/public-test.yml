info:
  name: /public-test
  type: http
  seq: 1

http:
  method: GET
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/public-test"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
