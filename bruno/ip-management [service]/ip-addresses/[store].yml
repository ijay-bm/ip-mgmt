info:
  name: / [store]
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/ip-addresses"
  body:
    type: multipart-form
    data:
      - name: ip_address
        type: text
        value: fe80::1ff:fe23:4567:890a
      - name: label
        type: text
        value: Dawnguard
      - name: comment
        type: text
        value: Allergic to Fangs
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
