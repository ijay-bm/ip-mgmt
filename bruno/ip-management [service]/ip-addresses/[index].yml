info:
  name: / [index]
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/ip-addresses?per_page=20&page=1"
  params:
    - name: per_page
      value: "20"
      type: query
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
