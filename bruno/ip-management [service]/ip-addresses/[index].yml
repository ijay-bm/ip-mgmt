info:
  name: / [index]
  type: http
  seq: 3

http:
  method: GET
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/ip-addresses"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
