info:
  name: /id [show]
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/ip-addresses/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "2"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
