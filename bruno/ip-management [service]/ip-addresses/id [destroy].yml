info:
  name: /id [destroy]
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/ip-addresses/{{id}}"
  auth: inherit

runtime:
  variables:
    - name: id
      value: "3"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
