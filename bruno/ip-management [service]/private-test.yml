info:
  name: /private-test
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}{{IP_MANAGEMENT_SERVICE_PATH}}/private-test"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
