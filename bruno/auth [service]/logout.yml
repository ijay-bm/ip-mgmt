info:
  name: /logout
  type: http
  seq: 4

http:
  method: POST
  url: "{{URL}}{{AUTH_SERVICE_PATH}}/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
