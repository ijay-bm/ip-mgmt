info:
  name: /me
  type: http
  seq: 2

http:
  method: GET
  url: "{{URL}}{{AUTH_SERVICE_PATH}}/me"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
