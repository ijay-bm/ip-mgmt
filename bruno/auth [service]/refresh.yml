info:
  name: /refresh
  type: http
  seq: 3

http:
  method: POST
  url: "{{URL}}{{AUTH_SERVICE_PATH}}/refresh"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
